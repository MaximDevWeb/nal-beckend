name: Laravel Test
on:
  workflow_dispatch:

jobs:
  laravel-test:
    runs-on: ubuntu-latest

    steps:
      - name: create key
        run: |
          mkdir ~/.ssh
          echo "$remote_key" > ~/.ssh/id_github
          chmod 600 ~/.ssh/id_github

      - name: create file
        run: |
          echo "" > ~/text.txt
          chmod 600 ~/text.txt

      - name: deploy
        run: rsync -avzr --delete -e "ssh -p 22 -i ~/.ssh/id_github -o StrictHostKeyChecking=no" ~/text.txt a0761558@141.8.194.74:/home/a0761558/test/
